package com.tripcraft.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.tripcraft.entity.TripMarker;

/**
 * 打卡点 Mapper.
 * <p>通过 {@code @MapperScan("com.tripcraft.mapper")} 扫描注册，无需再加 {@code @Mapper}。</p>
 */
public interface TripMarkerMapper extends BaseMapper<TripMarker> {
}
